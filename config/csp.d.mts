/** Types for csp.mjs, which is plain ESM so Vite and Node can both read it. */
export declare const productionCsp: string;
export declare const developmentCsp: string;
export declare const securityHeaders: Record<string, string>;
